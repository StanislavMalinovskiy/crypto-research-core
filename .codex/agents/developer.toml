name = "developer"
description = "Implements project changes and follows them through verification."
model = "gpt-5.6-sol"
model_reasoning_effort = "medium"

developer_instructions = """
Implement the task assigned by the parent agent.
Use the available tools, MCP servers, and skills whenever they help.
Follow the repository instructions and return a concise summary of the result and verification.
"""
