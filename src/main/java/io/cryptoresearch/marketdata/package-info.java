/** Market-data ingestion, normalization and point-in-time observations. */
@ApplicationModule(allowedDependencies = "kernel::api")
package io.cryptoresearch.marketdata;

import org.springframework.modulith.ApplicationModule;
