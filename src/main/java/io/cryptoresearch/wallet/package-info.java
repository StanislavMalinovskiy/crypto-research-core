/** Point-in-time wallet performance and scoring. */
@ApplicationModule(allowedDependencies = { "kernel::api", "marketdata::api" })
package io.cryptoresearch.wallet;

import org.springframework.modulith.ApplicationModule;
