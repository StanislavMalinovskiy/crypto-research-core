/** Virtual positions, valuation and forward outcomes. */
@ApplicationModule(allowedDependencies = {
		"kernel::api", "marketdata::api", "strategy::api", "governance::api"
})
package io.cryptoresearch.measurement;

import org.springframework.modulith.ApplicationModule;
